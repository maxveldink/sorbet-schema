I need write permission to edit the file. The only change is renaming `parsed_json` → `parsed` on lines 12 and 15 — the JSON context is already obvious from the class name and `JSON.parse` call. No dead code was found; the AI analysis confirmed every line is exercised and structurally necessary.